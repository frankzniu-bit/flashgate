{
  "name": "@flashgate/domain",
  "version": "0.0.0",
  "private": true,
  "description": "Pure TypeScript domain layer for FlashGate: scheduler, toll engine, import parsers. Zero React Native imports.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~6.0.3",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "ts-fsrs": "^5.4.1"
  }
}
