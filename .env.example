# FlashGate is local-first and makes no network calls except user-initiated
# imports (§4/§8 of DESIGN_AND_BUILD_BRIEF.md). No secrets are required to
# build or run the app. This file exists to establish the .gitignore pattern
# ahead of any future env var needs (e.g. EAS project IDs).
